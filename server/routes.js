/*
 * Nose.JS Express WEB-server: Routes List
 */
'use strict';

var routesList = {
    "/"             : "server",
    "/server"       : "server",
    "/server.html"  : "server"   //< Test String
};

module.exports = routesList;
